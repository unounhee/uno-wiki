# 비즈니스 위키 운영 스키마 (CLAUDE.md)

이 문서는 이 위키에서 작업하는 LLM(클로드코드/코워크)이 반드시 따라야 하는 운영 규칙이다.
대화 중에 새로 정한 규칙·약속·예외는 그 세션 안에서 이 문서에 반영 문안을 제시하고 승인 후 기록한다. 여기 없는 규칙은 다음 세션에 사라진 것으로 간주한다.

## 1. 위키의 목적
- 운영자(우노)의 과외 비즈니스 지식·판단 기준·학생 데이터를 정제된 형태로 축적한다.
- 최종 목표: ①비즈니스 아이덴티티의 보존 ②후임자 인수인계 ③e-book 등 콘텐츠 원천 ④학원 전환 시 학업 관리 프로그램(AI 판단 엔진)의 원본 데이터 — 위키의 완성도가 곧 제품의 판단력이 된다.
- 위키는 로그 창고가 아니다. 정제된 지식만 담는다. (이 규칙은 콘텐츠에 대한 것이고, 작업 장부인 log.md는 예외다.)

## 2. 폴더 구조와 권한 계층
- core/ : 운영자 전용. 철학, 판단 기준, 전략. 생성·수정은 반드시 운영자 사전 승인(§3 전문 승인 사이클).
  - 교육철학.md (Q&A 원칙: 답을 직접 주지 않는다 등)
  - 판단기준.md (상황별 의사결정 원칙)
  - 미션설계철학.md
  - 사업전략.md (가격, 경쟁 분석, 학원 설계)
- ops/ : 후임자 열람 가능 영역. 실행 지식. 자동 처리 후 보고.
  - tutoring/ : 일반 과외 학생 페이지 + 수업 운영 매뉴얼
  - care-team/ : 관리팀 학생 페이지 + 미션 설계 매뉴얼 + 리포트 양식 + 위기 대응 프로토콜
  - shared/ : 학부모 소통 원칙·양식, 긴급 상황 대응 원칙
- inbox/ : 미분류 투하물, 승인 대기 항목. 자동 처리 후 보고.
- raw/ : 운영자 구술·원본 자료 보관소 (카파시 LLM Wiki 패턴). 규칙은 §4.
- index.md : **위키 페이지만** 나열하는 카탈로그. 폴더별로 묶어 각 페이지를 한 줄 요약과 함께 나열한다. raw 파일은 나열하지 않는다(원본 대장은 log.md). 모든 작업 후 갱신.
- log.md : 두 부분으로 구성.
  - 상단 "원본 대장": raw 파일마다 한 줄 `날짜 | raw 파일 | 한 줄 요약 | 반영된 위키 페이지`. Ingest마다 추가. 반영 열이 비어 있으면 아직 흡수되지 않은 원본이라는 뜻이다.
  - 하단 "작업 장부"(append-only): `날짜 [작업유형] 내용`. 작업유형: 설치 / ingest / lint / 개편 / 설정.
- 질문리스트.md : 답변 대기 큐. 규칙은 §7.
- 언어: 전부 한국어. 파일명도 한국어 허용.

## 3. Ingest(정보 반영) 규칙
사이클: raw 저장 → (대형 구술은 검증) → 정제 전문 제시 → 승인 → 반영 → index·log 갱신 → 커밋 → 보고.

- 새 정보(운영자 구술 포함)가 들어오면 **먼저 원문을 raw/에 그대로 저장**한 뒤 정제해 반영한다.
- 관련 페이지를 찾아 **제자리에서 갱신**한다. 정정·보강·사례 추가·질문에 대한 답변은 기존 페이지 수정으로 처리하고 새 페이지를 만들지 않는다.
- 새 페이지 생성:
  - 학생 페이지는 각 폴더의 템플릿 파일을 복사해 `학생이름.md`로 저장하고 보고한다.
  - 그 외 새 페이지는 만들지 말고 "새 페이지 후보"(제목·근거·들어갈 폴더)로 보고하고, 승인된 것만 만든다. 만들기 전에 index.md에 같은 개념을 다루는 페이지가 없는지 확인한다.
  - 위키 페이지 이름에 날짜·정정·답변·A1/A2 같은 사건 표지를 쓰지 않는다. 날짜는 raw 파일명과 log.md에만 쓴다.
- 정제 페이지에는 원문 출처를 표기한다. (예: `원문: raw/2026-07-23_수율철학.md`)
- core/ 반영은 **반영될 전문(최종 문안 그대로)**을 채팅에 제시하고 승인받은 뒤 파일 반영·커밋한다. 구조 요약이나 수정 목록만으로 커밋하지 않는다. 분량이 길어도 전문을 생략하지 않는다.
- 대형 구술 Ingest는 전문 제시 전에 원문 충실도와 기존 문서 정합성을 검증(검증 에이전트)한 뒤 제시한다.
- 분류가 애매한 정보는 임의 판단하지 말고 inbox/에 두고 질문한다.
- 관리팀 일일 로그는 위키에 넣지 않는다. 주 단위 요약·패턴만 학생 페이지에 반영한다.
- 모든 작업 후 index.md와 log.md를 갱신하고, 생성/수정한 파일 목록과 핵심 변경 내용을 요약 보고한다.

## 4. raw/ 규칙
- **불변.** LLM은 읽기만 하고 본문을 수정하지 않는다.
- 파일명 `YYYY-MM-DD_주제.md`. 날짜는 **실제 구술일**이어야 하며, 파일 생성 시 시스템 날짜를 확인해 명명한다.
- 기존 구술의 정정·교정은 원본 raw를 고치지 않고 별도 정정 raw(`YYYY-MM-DD_주제_정정.md`)로 저장해 원본에 잇는다. 갱신은 정제본과 규칙 문서만.
- 외부 자료 반입(카톡 대화 내보내기 등)은 `YYYY-MM-DD_반입_주제.md`로 저장한다. 날짜는 반입일로 하고, 자료가 다루는 기간은 파일 첫머리(반입 정보 블록)에 기재한다. 반입 정보 블록 아래의 원문은 그대로 두고 수정하지 않는다.
- 동일 구술이 중복 전송되면 raw를 중복 저장하지 않고 승인 확인만 진행한다.
- raw → 위키 반영 추적은 log.md 원본 대장의 "반영된 위키 페이지" 열로 한다. 기존 raw 상단의 `반영:` 목록은 그대로 두되, 앞으로는 원본 대장만 갱신한다.
- 위키의 모든 주장은 raw 파일로 거슬러 올라갈 수 있어야 한다.

## 5. 충돌·모순 처리
- 수치·서술이 최신 구술과 다르면: 규칙 문서(판단기준·대원칙 등)는 최신 구술 수치로 통일하고, 역사 서술·원문 인용부는 그대로 둔다. 바뀐 내용은 해당 페이지 하단 "변경 이력"에 날짜와 함께 남긴다.
- 철학·원칙과 모순되는 내용을 발견하면 덮어쓰지 말고 모순을 표시하고 보고한다.
- 판단이 서지 않으면 추측하지 말고 질문 목록으로 모아 묻는다.

## 6. 페이지 템플릿
### 과외 학생 페이지 (ops/tutoring/)
기본 정보(학년·과목·일정) / 현재 목표와 계획 / 성적·성장 추이 /
성향·학습 특성 / 학부모 소통 (이력·성향·선호 채널) / 다음 액션 / 특이사항

### 관리팀 학생 페이지 (ops/care-team/)
재수 목표(대학·전형)와 현재 위치 / 과목별 상태 /
미션 설계 원칙(이 학생 개인화 로직) / 주간 리듬·컨디션 패턴 /
멘탈·동기 추적 / 학부모 소통 / 위기 신호·대응 이력
(일일 로그는 넣지 않는다. 주 단위 요약·패턴만 반영한다.)

### 학부모 처리 원칙
- 기본: 학생 페이지 내 '학부모 소통' 섹션으로 관리.
- 승격 조건: 복수 학생과 연결되거나 독립적 관계 관리가 필요해지면
  독립 인물 페이지로 승격하고 학생 페이지에는 링크만 남긴다.

## 7. 질문리스트와 답변 루프
- 질문리스트.md: 운영자가 예고했거나 구술 중 "질문리스트에 킵"이라 지시한 주제를 출처와 함께 등재한다. 운영자가 **"질문리스트 열어"**라고 하면 목록을 열어 순차적으로 질문한다. 답변은 정식 Ingest로 반영한 뒤 [완료] 처리한다.
- 태그: [완료] 반영 완료 / [중요] 운영자가 "중요레벨로 남겨 놓으라"고 지시한 우선 항목 / [보류] 운영자가 지정한 시점까지 묻지 않는 항목.
- 답변 루프: 운영자가 답변을 주면 ①읽고 제대로 이해했는지 체크를 제시해 확인받고 ②커밋한 뒤 ③남은 질문을 기존 기호·번호(A2, B1 …) 그대로, 이유 포함 원문 그대로, 답변된 것만 제외하고 **전체**를 한 번에 회신한다. 임의로 몇 개만 추려서 보내지 않는다. 기호·번호는 영구 유지한다. 운영자는 한 번에 여러 답변을 줄 수 있다.
- 가격 관련 질문은 "과외 가격인지, 학원 가격인지, 공통 원리인지" 스코프를 명시해서 묻는다.

## 8. 문체·관점 가드레일
- 운영자의 철학과 어긋나는 서술 금지. 특히 "학생에게 답을 직접 주는" 방향의 기록·매뉴얼은 작성하지 않는다. 의심되면 질문한다.
- 사실과 해석을 구분해 기록한다. (예: "성적 하락(사실) / 동기 저하 추정(해석)")
- 검증 불가능한 자기평가·전해 들은 말·해석에는 (자평)·(전언)·(운영자 해석) 라벨을 붙인다.

## 9. 보안·반출·저장소
- 위키 내용을 외부 자료(e-book, 발표, 제3자 공유)로 옮길 때는 반드시 익명화한다.
- 학생·학부모 실명과 민감 정보는 위키 내부에서만 사용한다.
- 이 위키는 unolock 코드 저장소와 **별도의 git 저장소**다(학생 실명·민감정보 분리). GitHub 저장소는 비공개 유지, Public 전환 금지, collaborator 초대 신중. 저장소 생성과 공개 설정은 운영자가 직접 한다.

## 10. Git·도구
- 커밋 메시지에 큰따옴표(")를 쓰지 않는다(PowerShell 5.1에서 인자가 깨짐). 인용이 필요하면 홑낫표·작은따옴표로 대체하거나 표현을 바꾼다.
- 커밋 메시지는 한국어 + 작업유형 접두(ingest: / 구조: / 승격: 등).
- 운영자가 Obsidian에서 직접 수정한 내용은 다음 작업 때 확인해 커밋에 포함한다. .obsidian/은 gitignore.
- 파일 생성은 Write 도구로만 한다. Bash로 파일을 만들지 않는다.

## 11. Lint(정기 점검) — 운영자 요청 시 수행 (권장: 주 1회 또는 대형 Ingest 후)
- 3주 이상 갱신 없는 학생 페이지 목록
- 템플릿과 어긋난 페이지, 끊어진 링크, 모순 표시 항목 정리
- index.md와 실제 파일 목록의 불일치, index에 섞인 raw 항목, 원문 인용 누락 페이지
- 원본 대장에서 "반영된 위키 페이지"가 빈 raw 파일, 원본과 위키가 어긋나는 수치
- 같은 주제를 다루는 페이지 쌍, 어떤 위키 페이지에서도 링크되지 않은 고아 페이지, 이름에 날짜·사건 표지가 남은 위키 페이지
- 결과를 보고하고, 수정은 승인 후에 한다.

## Compact Instructions
압축 시 반드시 보존: 이 문서의 규칙 요지, 현재 작업 중인 페이지와 미완료 Ingest 항목, 승인 대기 중인 전문·새 페이지 후보·질문 목록, 질문리스트 답변 루프의 현재 위치.
